# 架構設計書

> 本文件由 Phase 1: ARCHITECT 產出，需使用者確認後才進入開發計畫階段。
> 輸入：`01_spec.md`

## 基本資訊

| 欄位 | 內容 |
|------|------|
| 功能名稱 | <!-- 同 spec --> |
| 架構師 | architect agent |
| 建立日期 | <!-- YYYY-MM-DD --> |

## 系統架構總覽

<!-- 用文字或 ASCII 圖描述整體架構 -->

```
<!-- 例：
install.py (獨立入口，零依賴)
    ├── Step 1: 環境檢查
    ├── Step 2: gh CLI 安裝
    └── Step 3: clone + 啟動 setup.py

setup.py (專案內入口)
    ├── setup/wizard.py (互動流程)
    ├── setup/github_cli.py (gh 安裝邏輯)
    └── setup/telegram.py (Telegram 設定)
-->
```

## 模組拆分

| 模組 | 檔案 | 職責 | 依賴 |
|------|------|------|------|
| <!-- 模組名 --> | <!-- 檔案路徑 --> | <!-- 單一職責描述 --> | <!-- 依賴哪些模組 --> |

## 技術選型

| 決策 | 選擇 | 理由 | 替代方案 |
|------|------|------|---------|
| <!-- 例：HTTP 下載 --> | urllib.request | 零依賴 stdlib | requests（需安裝） |

## 介面定義

<!-- 模組之間的關鍵介面 / 函式簽名 -->

```python
# 例：
def setup_github_cli() -> bool:
    """安裝並驗證 gh CLI。成功回傳 True。"""
    ...
```

## 資料流

```
<!-- 描述資料如何在模組間流動 -->
<!-- 例：使用者輸入 → wizard.py → github_cli.py → 系統 PATH -->
```

## 風險評估

| 風險 | 影響 | 緩解措施 |
|------|------|---------|
| <!-- 例：winget 不可用 --> | gh 安裝失敗 | 3 層備援：winget → MSI → zip |

---

**使用者確認：** [ ] 已確認架構設計，可進入開發計畫階段
